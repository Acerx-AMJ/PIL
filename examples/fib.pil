fib(n) let minus1, minus2, fib1, fib2, result
   leeq n, 1, $0
   jmp $0, fib-branch

   add n, -1, minus1
   add n, -2, minus2

   call fib1, fib, minus1
   call fib2, fib, minus2
   add fib1, fib2, result
   return result

fib-branch:
   return n

main()
   call $0, fib, 30
   printn $0
